name: CI/CD Deploy to Linux

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout code từ nhánh main
        uses: actions/checkout@v3
        with:
          ref: main

      - name: 🧳 Copy code lên server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: 8686         
          source: "./"
          target: "/home/GreenMap-Frontend"

      - name: 🚀 Deploy Docker trên server
        id: deploy_server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: 8686         
          script: |
            cd /home/GreenMap-Frontend
            docker compose down || true
            docker compose build --no-cache
            docker compose up -d

      # ================================
      # 📣 THÔNG BÁO THÀNH CÔNG
      # ================================
      - name: 📣 Thông báo deploy thành công (Discord)
        if: success()
        run: |
          COMMIT_MSG="${{ github.event.head_commit.message }}"
          AUTHOR="${{ github.event.head_commit.author.name }}"
          PIPELINE_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          curl -H "Content-Type: application/json" \
          -d "{
            \"content\": \"@everyone 🚀 **Deploy thành công!**\",
            \"embeds\": [{
              \"title\": \"GreenMap-Frontend đã deploy thành công\",
              \"description\": \"Dự án đã chạy phiên bản mới nhất từ nhánh **main**.\",
              \"color\": 5763719,
              \"fields\": [
                {\"name\": \"📝 Commit\", \"value\": \"${COMMIT_MSG}\"},
                {\"name\": \"👤 Tác giả\", \"value\": \"${AUTHOR}\"},
                {\"name\": \"🔗 Pipeline\", \"value\": \"[Xem pipeline tại đây](${PIPELINE_URL})\"},
                {\"name\": \"🕒 Thời gian\", \"value\": \"$(date)\"}
              ]
            }]
          }" \
          ${{ secrets.DISCORD_WEBHOOK }}

      # ================================
      # ❌ THÔNG BÁO THẤT BẠI
      # ================================
      - name: ❌ Thông báo deploy thất bại (Discord)
        if: failure()
        run: |
          PIPELINE_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          curl -H "Content-Type: application/json" \
          -d "{
            \"content\": \"@everyone ❌ **Deploy thất bại!**\",
            \"embeds\": [{
              \"title\": \"Lỗi trong quá trình deploy GreenMap-Frontend\",
              \"description\": \"Vui lòng kiểm tra lại logs của GitHub Actions.\",
              \"color\": 15548997,
              \"fields\": [
                {\"name\": \"🔗 Pipeline\", \"value\": \"[Xem lỗi tại đây](${PIPELINE_URL})\"},
                {\"name\": \"🕒 Thời gian\", \"value\": \"$(date)\"}
              ]
            }]
          }" \
          ${{ secrets.DISCORD_WEBHOOK }}
